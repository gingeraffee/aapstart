# URL of the FastAPI backend.
# The Next.js rewrite rule proxies /api/* to this URL,
# so cookies work correctly on localhost.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Set to true to show the local dev login shortcut on the sign-in page.
NEXT_PUBLIC_DEV_AUTH_BYPASS=false
